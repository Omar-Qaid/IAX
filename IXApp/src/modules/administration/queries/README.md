# Administration queries

`settingsQueries.ts` exposes TanStack Query operations and `settingsQueryKeys.ts` owns stable settings cache keys. Invalidate using these keys rather than inline arrays.

[Administration module](../README.md)
