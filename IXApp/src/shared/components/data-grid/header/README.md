# DataGrid header

Provides sortable/pinned header cells, menus, and filter input/popover presentation. The header emits sort/filter changes to grid state hooks and does not own server queries.

`index.ts` exposes the local header implementation to the parent grid.

[DataGrid](../README.md) · [Grid hooks](../hooks/README.md)
