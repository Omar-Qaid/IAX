# Core utilities

Pure reusable helpers for dates, enums, formatting, objects, strings, and validation. These files contain no React state and should remain independent of shared UI and business modules.

Add a helper only when it has multiple infrastructure-level consumers; otherwise keep it near its feature.

[Core](../README.md) · [Shared utilities](../../shared/utilities/README.md)
