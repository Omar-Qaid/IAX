# Navigation components

Contains the reusable app-owned navigation presentation: `NavItem`, `NavSection`, and `PageBreadcrumbs`. `navigationTokens.ts` holds presentation tokens and `index.ts` is the local export boundary. These components consume route/navigation metadata; they do not register routes.

[Application shell](../../../docs/app-shell.md) · [Routes](../routes/README.md)
