# Workflow lookups

`processLookup.ts` defines the domain-specific process lookup configuration consumed by workflow UI. Generic lookup rendering remains in shared components.

[Workflow module](../README.md)
