# Dashboard pages

Contains the route-level `DashboardPage.tsx`. It is the module's current implementation; there is no separate dashboard API, query, or pattern folder.

[Dashboard module](../README.md)
