# Administration API

Contains `settingsApiRepository.ts` for remote settings persistence and `sysNumberSequenceApi.ts` for number-sequence requests. Pages access these through the module's query/service flow rather than directly where a service exists.

[Administration module](../README.md)
