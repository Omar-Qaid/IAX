# Core constants

Contains generic constants for the application (`appConstants.ts`), date formats (`dateFormats.ts`), and persistence/session keys (`storageKeys.ts`). Business-specific constants should remain in their module; reusable UI constants belong under shared.

[Core](../README.md)
