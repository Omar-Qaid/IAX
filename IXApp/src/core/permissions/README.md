# Permissions

Defines permission identifiers and types, `permissionService` checks, and `usePermission`. Auth hooks provide the current user's permission set; route and component guards consume these checks.

Only document or enforce a permission when it exists in the real route/action flow.

[Authentication and authorization](../../../docs/authentication.md) · [Action pane](../../shared/components/action-pane/README.md)
