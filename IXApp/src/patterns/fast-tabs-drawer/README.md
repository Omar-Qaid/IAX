# Fast Tabs Drawer

Reusable quick-create composition built on the existing fast-tabs form behavior.

- Opens from the right in LTR layouts and from the left in RTL layouts while its content follows the active application direction.
- Preserves validation, unsaved-change protection, loading, errors, and save actions.
- Receives fields and lookup data from the consuming module; it contains no business-specific logic.
