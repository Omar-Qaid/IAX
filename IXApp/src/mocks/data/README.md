# Shared mock datasets

Exports typed in-memory records from `currencies.ts`, `customerGroups.ts`, `customers.ts`, and `salesOrders.ts`. These are data fixtures, not a generic repository framework. Mutating mock behavior is implemented by the feature adapter or page data source that consumes them.

[Mocks](../README.md) · [Testing](../../../docs/testing.md)
