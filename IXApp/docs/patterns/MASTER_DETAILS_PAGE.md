# Master-detail status

`src/patterns/master-detail/MasterDetailLayout.tsx` exists, but `MasterDetailPage.tsx`, `types.ts`, and `useMasterDetailPage.ts` are empty. The folder is a partial scaffold, not a production pattern.

Current pages that need related tables generally use enterprise `ListDetailsPage`, detail-section content, and `TabularDetailPanel`. Complete the scaffold only after defining parent/child persistence, selection, empty state, responsive layout, dirty coordination, and tests.
