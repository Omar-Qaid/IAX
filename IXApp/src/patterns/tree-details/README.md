# Tree Details pattern

`TreeNavigation.tsx` implements tree navigation, while `TreeDetailsPage.tsx` and `types.ts` are incomplete. There is no established production feature proving a complete tree/details contract.

Do not adopt it as a full pattern until selection, loading, empty states, responsive behavior, and tests are completed.

[Patterns](../README.md)
