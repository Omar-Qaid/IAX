# Administration services

`settingsService.ts` selects the HTTP or mock settings repository using runtime mock configuration and exposes the feature-facing settings contract.

[Administration module](../README.md)
