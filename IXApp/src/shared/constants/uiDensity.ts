/** Shared compact-density measurements used by application-wide UI primitives. */
export const uiDensity = {
  controlHeight: 30,
  buttonHeight: 28,
  toolbarHeight: 36,
  tabHeight: 34,
  gridRowHeight: 32,
  gridHeaderHeight: 64,
  gridFooterHeight: 28,
  dialogPadding: 12,
  sectionPadding: 10,
  pageGap: 4,
  scrollbarSize: 6,
} as const;
