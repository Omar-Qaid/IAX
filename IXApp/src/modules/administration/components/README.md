# Administration components

Feature-owned `GlobalSettingsForm` and `UserSettingsForm` components. They compose shared fields/forms with administration types and schemas; they are not general shared controls.

[Administration module](../README.md)
